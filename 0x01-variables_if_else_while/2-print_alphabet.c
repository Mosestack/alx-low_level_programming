#include <stdio.h>
/**
 * main - a program that prints lowercase alphabets
 * Return: 0 (Success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'Z') {
			putchar(c);
				c++;
		}
	putchar('\n');
	return (0);
}
